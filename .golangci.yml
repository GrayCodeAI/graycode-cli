run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - ineffassign
    - unused
    - errcheck
    - staticcheck
    - gosec
    - nilerr
    - misspell

linters-settings:
  errcheck:
    check-type-assertions: true
  gosec:
    excludes:
      - G204
      - G304
  misspell:
    locale: US
  staticcheck:
    checks: ["all", "-U1000"]

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-dirs:
    - vendor
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
