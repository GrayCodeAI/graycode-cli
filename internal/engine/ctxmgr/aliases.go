// Package ctxmgr is the namespace for context budget, decay, packing,
// providers, visualisation, and read-only context. See ../../docs/plans/engine-refactor-plan.md.
//
// Named "ctxmgr" (not "context") to avoid shadowing the stdlib context package.
package ctxmgr
